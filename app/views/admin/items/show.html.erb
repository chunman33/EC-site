<div class="container">
  <div class="row">
    <div class="col-12">
      <h5>商品詳細</h5>
    </div>
  </div>
    <div class="row">
      <div class="col-md-4 mt-5">
        <%=image_tag @item.get_image(300, 300) %>
      </div>
      <div class="col-md-8 mt-5">
      <table style="width: 100%; height: 100%;"><!--インラインでCSSを指定-->
        <tbody>
          <tr><td>商品名</td><td class="date"><%= @item.name%></td></tr>
          <!--dateクラスにCSSでfont-sizeを指定して文字を拡大-->
          <tr><td>商品説明</td><td class="date"><%= @item.description%></td></tr>
          <tr><td>ジャンル</td><td class="date"><%= @item.genre.name%></td></tr>
          <tr><td>税込価格(税抜価格)</td>
          <td class="date"><%= @item.add_tax_price.to_s(:delimited) %><!--.to_s(:delimited)は３桁ごとにカンマ区切りで表示してくれるメソッド-->
        (<%= @item.excluding_tax_price.to_s(:delimited) %>)円 </td></tr>
          <tr><td>販売ステータス</td>
          <% if @item.sale_status? == false %>
            <td class="date" style ="color:#C0C0C0">販売停止中</td>
              <% else %>
              <td class="date" style ="color:#00FF00">販売中</td>
            <% end %></tr>
            </tr>
        </tbody>
      </table>
      </div>
      
      
       </div>
      </div>
      <div class="text-center mt-5">
        <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-success"%>
      </div>
    
  </div>
